# Markdown Converter

This is a minimal static web app that converts Markdown to HTML using [marked.js](https://marked.js.org/).

## How to run

Open `index.html` in a browser. The sample Markdown is automatically converted and displayed in the page.

## Features

- Uses CDN to load marked.js.
- No build tools or server required.
- Simple, clean UI.

## License

MIT.